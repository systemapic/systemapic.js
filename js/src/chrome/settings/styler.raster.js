Wu.RasterStyler = Wu.Class.extend({

	initialize : function (options) {

		console.error('initialize RasterStyler');


		// set options
		// Wu.setOptions(this, options);

		// init container
		// this._initContainer();
	},

})